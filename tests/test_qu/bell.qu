import std_ops

operation bell_state() : bool[] {
    bool[2] res;
    using(q0: qubit, q1: qubit) {
        H(q0);
        CNOT(q0,q1);
        res[0] = measure(q0);
        res[1] = measure(q1);
    }
    return res;
}
