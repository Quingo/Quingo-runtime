import std_ops

operation bell_state() : unit {
    using(q : qubit[2] = {42,48}) {
        H(q[0]);
        CNOT(q[0], q[1]);
        measure(q[0]);
        measure(q[1]);
    }
}
operation main(): unit{
    return bell_state();
}